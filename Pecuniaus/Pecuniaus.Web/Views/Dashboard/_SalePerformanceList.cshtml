@model  IEnumerable<Pecuniaus.Models.TotalSale>
@using GridMvc.Html

@Html.Grid(Model).Columns(columns =>
{
    columns.Add(m => m.Description).Titled("Description");
    columns.Add(m => m.LoanedAmount).Titled("MCA Amount");
}).WithPaging(5).Sortable(true)
